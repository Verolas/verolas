{
  "name": "verolas",
  "version": "0.0.0",
  "private": true,
  "description": "Verolas monorepo root. Vertical agentic AI platform for civil engineering.",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
