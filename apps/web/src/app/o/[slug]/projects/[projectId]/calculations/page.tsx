import { ProjectPlaceholder } from "@/components/app-shell/project-placeholder";

export default function CalculationsPage() {
  return (
    <ProjectPlaceholder
      title="Calculations"
      description="Reproducible engineering calculation runs, versioned and audited."
      body="Calc runs land here with inputs, code, results, and a signed snapshot of the reviewer's sign-off. Comparable to a notebook with full provenance."
    />
  );
}
