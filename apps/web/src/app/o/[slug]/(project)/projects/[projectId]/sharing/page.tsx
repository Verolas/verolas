import { ProjectPlaceholder } from "@/components/app-shell/project-placeholder";

export default function SharingPage() {
  return (
    <ProjectPlaceholder
      title="Sharing"
      description="Read-only showcase URLs you can hand to a client or building official."
      body="Generate a watermarked, time-bounded view of the latest signed deliverables — drawings, calc package summary, sustainability report — without giving external parties access to the live project. Every view is audited."
    />
  );
}
