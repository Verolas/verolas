import { ProjectPlaceholder } from "@/components/app-shell/project-placeholder";

export default function CommunicationsPage() {
  return (
    <ProjectPlaceholder
      title="Communications"
      description="One inbox for RFIs, comments, transmittals, and parsed emails."
      body="Verolas Connect parses RFIs from Outlook and Gmail, every comment thread on a drawing or calc rolls up here, and outbound transmittals to the architect or client are tracked with delivery receipts."
    />
  );
}
