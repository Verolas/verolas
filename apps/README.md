# apps/

Top level user facing applications in the Verolas monorepo.

- `web` Next.js 15 web app
- `api` FastAPI gateway

Both directories ship as placeholders today so Turborepo and pnpm can resolve them. Real code lands as each workstream comes online.
