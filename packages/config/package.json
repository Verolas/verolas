{
  "name": "@verolas/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript, ESLint, Prettier, and Tailwind config for all Verolas TS workspaces.",
  "type": "module",
  "exports": {
    "./tsconfig.base.json": "../../tsconfig.base.json"
  },
  "scripts": {
    "lint": "echo 'config lint placeholder'",
    "typecheck": "echo 'config typecheck placeholder'",
    "clean": "rm -rf .turbo"
  }
}
